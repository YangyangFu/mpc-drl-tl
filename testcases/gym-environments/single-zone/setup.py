from setuptools import setup

setup(name='gym_singlezone_jmodelica',
      version='0.0.1',
      install_requires=['gym']  # And any other dependencies foo needs
)

setup(name='gym_singlezone_rom',
      version='0.0.1',
      install_requires=['gym']  # And any other dependencies foo needs
)
